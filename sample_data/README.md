Sample files for API upload testing (no frontend mock data):

- `sap_sample.csv` — SAP-style procurement/fuel (mixed headers/units)
- `utility_sample.csv` — portal electricity export
- `travel_sample.json` — Concur/Navan-style bookings

Upload via **Upload Center** in the app after running `python manage.py seed_demo`.
